apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: keycast-unified
  annotations:
    run.googleapis.com/ingress: all
    run.googleapis.com/launch-stage: GA
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/execution-environment: gen2
        # Set to "false" for continuous replication (slightly higher cost)
        # Set to "true" to save $ (replication may lag between requests)
        run.googleapis.com/cpu-throttling: "false"
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      serviceAccountName: PROJECT_ID@appspot.gserviceaccount.com  # Will be replaced by deploy script
      volumes:
        - name: database
          emptyDir:
            sizeLimit: 500Mi
        - name: litestream-config
          secret:
            secretName: litestream-config
            items:
              - key: latest
                path: litestream.yml
      containers:
        - name: keycast-unified
          image: IMAGE_URL  # Will be replaced by deploy script
          ports:
            - containerPort: 3000
          # Runs both API and Signer in same container
          args: ["unified"]
          env:
            - name: DATABASE_PATH
              value: "/data/keycast.db"
            - name: NODE_ENV
              value: "production"
            - name: RUST_LOG
              value: "info"
            - name: WEB_BUILD_DIR
              value: "/app/web"
            - name: USE_GCP_KMS
              value: "true"
            - name: GCP_PROJECT_ID
              value: "openvine-co"
            - name: CORS_ALLOWED_ORIGIN
              value: "https://login.divine.video"
            - name: APP_URL
              value: "https://login.divine.video"
            - name: FROM_EMAIL
              value: "noreply@divine.video"
                  name: keycast-ucan-secret
                  key: latest
            - name: SENDGRID_API_KEY
              valueFrom:
                secretKeyRef:
                  name: keycast-sendgrid-api-key
                  key: latest
            - name: MASTER_KEY_PATH
              valueFrom:
                secretKeyRef:
                  name: keycast-master-key
                  key: latest
          volumeMounts:
            - name: database
              mountPath: /data
          resources:
            limits:
              cpu: "2000m"
              memory: "2Gi"
          startupProbe:
            httpGet:
              path: /health
              port: 3000
            failureThreshold: 30  # 30 retries * 3s = 90s for DB restore + startup
            periodSeconds: 3
            timeoutSeconds: 2

        - name: litestream
          image: litestream/litestream:0.3
          args: ["replicate", "-config", "/config/litestream.yml"]
          volumeMounts:
            - name: database
              mountPath: /data
            - name: litestream-config
              mountPath: /config
          resources:
            limits:
              cpu: "200m"
              memory: "256Mi"
